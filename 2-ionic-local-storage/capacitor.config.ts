import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'ionic-demo-1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
